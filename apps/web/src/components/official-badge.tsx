/** Only seller-like label ever shown to buyers (v2 platform_owned) */
export function OfficialBadge() {
  return (
    <span className="inline-flex items-center gap-1 rounded-full bg-mn-teal-soft px-2.5 py-0.5 text-[10px] font-bold uppercase tracking-wide text-mn-teal">
      MarketNest Official
    </span>
  );
}
